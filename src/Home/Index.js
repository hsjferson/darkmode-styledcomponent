import React, { Component } from 'react';
import { View,Text } from 'react-native';

import {Area, Texto} from './Styles';

export default class Home extends Component {
 render(){
  return (
    <Area>
        <Texto>Inicio</Texto>

        
    </Area>
   );
 }
}